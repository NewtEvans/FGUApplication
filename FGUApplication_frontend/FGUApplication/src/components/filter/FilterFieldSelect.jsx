import InputSelect from "../components/InputSelect";
export default function FilterFieldSelect(
  name,
  label,
  items,
  showLabel,
  register
) {
  return (
    <div>
      <InputSelect />
    </div>
  );
}
